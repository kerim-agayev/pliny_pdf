# Wave 4C — Annotation features in new editor

Pending (after 4B). Migrate annotation features from the old editor into the new
one, client-side overlay + burned into the PDF on save (server-side):
highlight, strikethrough, underline, comment/sticky note, freehand drawing,
shapes (rect/circle/arrow/line), link insertion.

GATE 4C: all annotation tools work and save correctly into the output PDF.
