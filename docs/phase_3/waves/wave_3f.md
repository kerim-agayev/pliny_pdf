# Wave 3F — UX Polish

(Stub — planned, not started.)

- Toast wiring across all 28 tools (replace inline banners/console errors).
- ToolStatus wiring across all 28 tools (progress prop for page-by-page).
- Keyboard shortcuts (Ctrl/Cmd+O open, Ctrl/Cmd+D download, Esc cancel) via ToolShell.
- Recent files: `lib/recentFiles.ts` (localStorage, max 10) + `components/shared/RecentFiles.tsx`,
  wired into dashboard + /tools.
