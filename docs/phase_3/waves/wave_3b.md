# Wave 3B — Limits & Security

(Stub — planned, not started.)

- File-size limits: local 100MB; cloud anon 25 / free 50 / pro 200 MB (backend + dropzone).
- Split "every page" capped at 500; range mode unlimited.
- PDF→JPG: size-limit only, no page cap.
- Magic-byte validation enforced.
- Password prompt on every tool when uploaded PDF is encrypted (PasswordModal from 3A).
