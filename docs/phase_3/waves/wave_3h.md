# Wave 3H — PDF→Word patience UI

(Stub — planned, not started.)

- Default Option B: patience UI + keep Gotenberg.
- >50 pages: info toast before processing; >200 pages: inline warning banner.
- 5-min timeout handling message. Document decision in `decisions.md`.
